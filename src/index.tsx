import React from "react";
import ReactDOM from "react-dom";
import App from "src/App";

// FIXME: ReactDOM.render is not supported
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
